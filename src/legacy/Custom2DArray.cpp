#include <vector>

// std::pair<int, int> indexToCoords(int h, int w, int index) {
//   return std::pair(index % w, (index - (index % w)) / w);
// };

// int coordsToIndex(int h, int w, std::pair<int, int> coords) {
//   return coords.second * w + coords.first;
// };